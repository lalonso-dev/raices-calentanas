{% extends '_layouts' %}

{% set categoryId = '' %}
{% set category = category ?? null %}
{% if category %}
  {% set categoryId = category.id %}
  {% set title = "Explora nuestro catálogo de #{category.title}" %}
{% else %}
  {% set title = entry.itemTitle ?? '' %}
{% endif %}

{% block content %}
  {{
    sprig(
      'components/bodyResources/index',
      {
        title: title,
        categoryId: categoryId
      }
    )
  }}

  {{ craft.vite.script('templates/pages/%s/script.js'|format('resources')) }}
{% endblock %}
